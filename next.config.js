/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "s2.googleusercontent.com",
      "unsplash.com",
      "images.unsplash.com",
    ],
  },
}

module.exports = nextConfig
